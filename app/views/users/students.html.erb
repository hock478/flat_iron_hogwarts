<div id="students_show">
    <div id="student_directory">
    <h2>Student Directory</h2>
    <% @students.order('lower(name) ASC').each do |student| %>
    <div><%= link_to student.name, user_path(student) %></div>
    <% end %>
    </div>
</div>